---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: optimizationsuggestions.aiops.ggai.dev
spec:
  group: "aiops.ggai.dev"
  names:
    kind: OptimizationSuggestion
    listKind: OptimizationSuggestionList
    plural: optimizationsuggestions
    singular: optimizationsuggestion
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OptimizationSuggestion is the Schema for the optimizationsuggestions
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OptimizationSuggestionSpec defines the desired state of OptimizationSuggestion.
            properties:
              categories:
                description: Categories to analyze.
                items:
                  type: string
                type: array
              scope:
                description: Scope of the optimization analysis.
                properties:
                  namespace:
                    description: Namespace for namespace/workload scope.
                    type: string
                  type:
                    description: 'Type: cluster, namespace, workload.'
                    enum:
                    - cluster
                    - namespace
                    - workload
                    type: string
                  workload:
                    description: Workload for workload scope.
                    type: string
                required:
                - type
                type: object
              window:
                default: 7d
                description: Window is the analysis time range.
                type: string
            required:
            - scope
            type: object
          status:
            description: OptimizationSuggestionStatus defines the observed state.
            properties:
              analyzedAt:
                description: AnalyzedAt timestamp.
                format: date-time
                type: string
              phase:
                description: 'Phase: Pending, Analyzing, Completed, Failed.'
                enum:
                - Pending
                - Analyzing
                - Completed
                - Failed
                type: string
              suggestions:
                description: Suggestions from the analysis.
                items:
                  description: Suggestion is a single optimization recommendation.
                  properties:
                    confidence:
                      description: Confidence score (0.0 - 1.0).
                      type: number
                    current:
                      additionalProperties:
                        type: string
                      description: Current state (e.g. resource requests/limits).
                      type: object
                    description:
                      description: Description of the suggestion.
                      type: string
                    estimatedSavings:
                      description: EstimatedSavings description (e.g. "60% cost reduction").
                      type: string
                    priority:
                      default: medium
                      description: 'Priority: critical, high, medium, low.'
                      enum:
                      - critical
                      - high
                      - medium
                      - low
                      type: string
                    recommended:
                      additionalProperties:
                        type: string
                      description: Recommended state.
                      type: object
                    risk:
                      default: low
                      description: Risk of applying this suggestion.
                      enum:
                      - low
                      - medium
                      - high
                      - critical
                      type: string
                    target:
                      description: Target workload.
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                    type:
                      description: Type of optimization.
                      enum:
                      - resource-rightsize
                      - hpa-recommendation
                      - pdb-recommendation
                      - cost-reduction
                      - performance
                      - reliability
                      - security
                      type: string
                  required:
                  - description
                  - priority
                  - risk
                  - type
                  type: object
                type: array
              summary:
                description: Summary message.
                type: string
              totalEstimatedSavings:
                description: TotalEstimatedSavings aggregated savings estimate.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
